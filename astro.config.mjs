// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
const site = "https://lilianleexr.github.io";
const base = "/";

export default defineConfig({
  site,
  base,
});
